import React from 'react';
import ReactDOM from 'react-dom';

import './test.css';

import { a } from '../../lib/aa';

console.log(a);

const root = document.getElementById('main');

ReactDOM.render(
    (
        <div>demo233</div>
    ),
    root
);